import {createTheme} from "@mui/material";

const Theme = createTheme({
    palette: {
        primary: {
            main: '#eef0f2'
        },
        secondary: {
            main: '#35393d'
        }
    }
})

export default Theme